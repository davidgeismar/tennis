<div class="content text-center">
  <div role="tabpanel">

    <!-- Nav tabs -->
    <ul class="nav nav-tabs" role="tablist">
      <li id="profi-nav-tab" role="presentation" class="active"><a href="#profile" aria-controls="flats" role="tab" data-toggle="tab">Mes" aria-controls="profile" role="tab" data-toggle="tab">profile</a></li>
      <li id="tournois-nav-tab" role="presentation"><a href="#tournois" aria-controls="flats" role="tab" data-toggle="tab"</a></li>
    </ul>

     <!-- Tab panes -->
    <div class="tab-content">
      <div role="tabpanel" class="tab-pane fade in active" id="profile">
        <div class="card">
              <h4 class= "text-left"> Prénom : <%= @user.first_name %></h4>
              <h4 class= "text-left"> Nom :<%= @user.last_name %></h4>
              <h4 class= "text-left"> email : <%= @user.email %></h4>
              <h4 class= "text-left"> Numéro de juge-arbitre : <%= @user.judge_number %></h4>
              <h4 class= "text-left"> Sexe : <%= @user.genre %></h4>
              <h4 class= "text-left">Date de naissance : <%= @user.date_of_birth %></h4>
              <h4 class= "text-left">Numéro de licence : <%= @user.licence_number %></h4>


              <%= link_to "Modifier mon profil", edit_user_path  %>

      <div role="tabpanel" class="tab-pane fade" id="tournois">
        <div class ="card">

        </div>
      </div>
    </div>
    </div>
  </div>
</div>

