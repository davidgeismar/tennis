<div class="banner padded">
  <div class="container">
    <div class="row">
      <div class="col-xs-6 col-xs-offset-3 bigmarge">
        <%= simple_form_for(@user) do |f| %>
          <%= f.error_notification %>
          <p>
            <%= f.label :picture, label: "Photo" %>
            <div class="upload-file-container">

              <%= f.file_field :picture, id: "myFileInput4" %>
            <span id="fileName4"></span>
            </div>
          </p>
          <%= f.input :first_name, label: "Prénom" %>
          <%= f.input :last_name, label: "Nom" %>
          <%= f.input :email, label: "email" %>
          <%= f.input :telephone %>
          <%= f.input :judge_number, label: "Numéro de juge" %>
          <p><%= f.label :birthdate, 'Date de naissance' %></p>
        <%= f.date_select :birthdate, {:include_blank => true, :default => nil, :use_month_names => ['Janv.','Fevr.', 'Mars', 'Avr.', 'Mai', 'Juin', 'Juil.', 'Août','Sept.', 'Oct.', 'Nov.', 'Déc.'], :order => [:day, :month, :year], :start_year => 1910, :end_year => 1995} %>

          <%= f.input :iban, label: "Votre Iban", placeholder: "FR70 3000 2005 5000 0015 7845 Z02" %>
          <%= f.input :bic, label: "Votre bic", placeholder: "AXABFRPP"%>
          <%= f.input :address, label: "Votre addresse" %>
          <%= f.button :submit, "Valider", class: "btn-success" %>

        <% end %>
      </div>
    </div>
  </div>
</div>

<% content_for(:after_js) do %>
<script>
  $(document).ready(function(){
      realInputField = $('#myFileInput4')
      realInputField.change(function() {
        $('#fileName4').append( "<i class='fa fa-check'></i>" )
      });
   });
</script>
<% end -%>
